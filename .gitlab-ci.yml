workflow:
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH


stages:
  - build
  - test
  - deploy

build-code-job:
  stage: build
  script:
    - echo "Check the ruby version, then build some Ruby project files:"

test-code-job1:
  stage: test
  script:
    - echo "If the files are built successfully, test some files with one command:"


test-code-job2:
  stage: deploy
  image: helm
  script:
    - helm repo add jhidalgo3-github https://jhidalgo3.github.io/helm-charts/
    - helm install my-hello-kubernetes-chart jhidalgo3-github/hello-kubernetes-chart --version 3.0.0 -n hello-test